[project]
name = "prac"
version = "0.1.0"
description = "Leetcode spaced rep tracker"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "click>=8.3.0",
    "supermemo2>=3.0.1",
]

[project.scripts]
prac = "prac.lc:cli"
